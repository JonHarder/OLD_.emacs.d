;;; prose.el --- Configuration for personal prose-mode. -*- lexical-binding: t -*-

;;; Commentary:

;;; Code:
(require 'prose-mode "~/.emacs.d/ext_lisp/prose-mode.el")

(defun modules/prose--load (config)
  "Set up `prose-mode' using argument CONFIG.")

(provide 'prose)
;;; prose.el ends here
