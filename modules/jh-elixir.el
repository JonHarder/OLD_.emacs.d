;;; elixir --- Summary
;; All things related elixir language

;;; Commentary:

;;; Code:

(defun modules/elixir--load (config)
  (use-package elixir-mode
    :mode "\\.exs|\\.elixir"))

(provide 'jh-elixir)
;;; jh-elixir.el ends here
